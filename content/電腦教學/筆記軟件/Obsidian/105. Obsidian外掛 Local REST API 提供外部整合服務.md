
安裝並啟用了 `Local REST API` 外掛後，當 `Obsidian` 啟動後同時會啟用提供 `Local REST API` 服務的伺服器，本機的應用程式就可以對它送出下列 `HTTP` 請求：
![[assets/105. Obsidian外掛 Local REST API 提供外部整合服務/Pasted image 20260607222617.png]]

**`Local REST API` 插件的設置:**
1. 需要勾選 `Enable non-encrypted (HTTP) server`。
   ![[assets/105. Obsidian外掛 Local REST API 提供外部整合服務/Pasted image 20260608102210.png]]
   `Non-encrypted (HTTP) API URL: http://127.0.0.1:27123` 才會生效。 
   ![[assets/105. Obsidian外掛 Local REST API 提供外部整合服務/Pasted image 20260608102350.png]]
2. 複製產生的 `API Key` 給 `Obsidian Web` 插件使用。

- HTTPS請求：[https://127.0.0.1:27124/](https://127.0.0.1:27124/)
- HTTP請求：[http://127.0.0.1:27123/](http://127.0.0.1:27123/)

當 `Local REST API` 服務的伺服器接收到 (`https://127.0.0.1:27124` 或 `https://127.0.0.1:27124`) 的請求後，就會根據請求的方法，自動對 `Obsidian` 的儲存庫作查詢、新增、修改的處裡。

先介紹設定步驟與當做本機 `Client` 端應用程式的 `Chrome` 瀏覽器擴充 [Obsidian Web](https://chromewebstore.google.com/detail/obsidian-web/edoacekkjanmingkbkgjndndibhkegad)，它主要是連接瀏覽器與你的 `Obsidian` 筆記。
安裝完成作釘選並且開啟**選項** 作對應的設定:

最後再用 `AutoHotkey` 撰寫另一個應用程式，在不彈出 `Obsidian` 視窗的狀況下，將輸入的訊息直接寫入指定的筆記。

## 目錄  
  
- [[#1. 設定準備]]  
- [[#2. 安裝 SSL 憑證]]  
- [[#3. API檢視]]  
- [[#4. 命令列測試]]
- [[#5. 網頁測試]]
- [[#6. Obsidian Web擴充選項]]
  - [[#6.1. 模板能使用的變數]]
  - [[#6.2. Create new note]]
  - [[#6.3. Patch a note]]
  - [[#6.4. Append to current daily note]]
  - [[#6.5. Capture page snapshot]]
  - [[#6.6. Append to existing note]]
  - [[#7. AutoHotkey整合腳本]]
  - [[#8. 相關鏈接]]
  - [[#9. 教學影片]]

## 1. 設定準備  

`API Key` 與 `SSL` 金鑰都是在安裝時隨機產生。
![[assets/105. Obsidian外掛 Local REST API 提供外部整合服務/Pasted image 20260607222936.png]]

1. 到 `Local REST API` 外掛選項裡複製 `API Key` 到剪貼簿
2. 點擊【download this certificate】鏈接或直接瀏覽 [https://127.0.0.1:27124/obsidian-local-rest-api.crt](https://127.0.0.1:27124/obsidian-local-rest-api.crt) 以取得外掛的自簽 `SSL` 憑證
   ![[assets/105. Obsidian外掛 Local REST API 提供外部整合服務/Pasted image 20260607223221.png]]

## 2. 安裝 SSL 憑證  

1. 點擊執行 `obsidian-local-rest-api.crt`
   ![[assets/105. Obsidian外掛 Local REST API 提供外部整合服務/Pasted image 20260607223314.png]]
2. 安裝憑證
   ![[assets/105. Obsidian外掛 Local REST API 提供外部整合服務/Pasted image 20260607223411.png]]
   ![[assets/105. Obsidian外掛 Local REST API 提供外部整合服務/Pasted image 20260607223556.png]]
   ![[assets/105. Obsidian外掛 Local REST API 提供外部整合服務/Pasted image 20260607223838.png]]
   ![[assets/105. Obsidian外掛 Local REST API 提供外部整合服務/Pasted image 20260607223917.png]]
3. 按完成
4. 【您要安裝此憑證？】回答是
5.  匯入執行成功

## 3. API檢視  

開啟[API說明文件](https://coddingtonbear.github.io/obsidian-local-rest-api/#)並瀏覽，以了解提供有那些 `API` 可以使用。

## 4. 命令列測試

`Windows cmd.exe` 裡用 `cUrl` 測試； `Linux` 或 `macOS` 將 `^` 改成 `\`：

```bash
  curl -X GET https://127.0.0.1:27124/vault/test.md ^
    -H "accept: text/markdown" ^
    -H "Authorization: Bearer aa70a05e7206b1b0e45462c92d878ae4a098e4cd3574f4745dc881e62d85985e"
```

## 5. 網頁測試

## 6. Obsidian Web 擴充選項

到 `Chrome` 線上應用程式商店安裝 `Obsidian Web` 並啟用
- [Obsidian Web - Chrome 線上應用程式商店](https://chrome.google.com/webstore/detail/obsidian-web/edoacekkjanmingkbkgjndndibhkegad?hl=zh-TW)
- 由網頁建立新的筆記
  - 到模板 `Templates` 點擊新增 **`+`**
    ![[assets/105. Obsidian外掛 Local REST API 提供外部整合服務/Pasted image 20260608174312.png]]

### 6.1. 模板能使用的變數

|網頁模板變數|說明|
|:--|:--|
|{{page.url}}|網頁網址|
|{{page.title}}|網頁標題|
|{{page.content}}|網頁內容|
|{{page.selectedText}}|選取文字|

|輔助模板變數|說明|
|:--|:--|
|{{date}}、{{date "Format"}}|系統日期時間|
|{{filename FIELD}}|FIELD是網頁模板變數|
|{{json FIELD}}|加上雙引號|
|{{quote FIELD}}|將指定變數開頭加上`>`|
|{{uuid}}||

[!INFO] 模板引擎  
[Handlebars](https://handlebarsjs.com/guide/)


#### 6.2. Create new note

- PUT /vault/{{filename page.title}}.md
- Content:

```
---
page-title: {{json page.title}}
url: {{page.url}}
date: "{{date}}"
---
{{#if page.selectedText}}

{{quote page.selectedText}}
{{/if}}
```

- 說明
    - {{json page.title}}: 網頁標題前後加上雙引號
    - 如果有選取文字，將選取文字開頭加上 `>`
    - 檔名可以用別的變數，例如`/vault/{{uuid}}.md`或`/vault/{{date "yyyyMMdd-HHmmss"}}.md`

### 6.3. Patch a note

搜尋符號的標題，將內容加在標 題的開頭或結尾處。

- PATCH /vault/檔名路徑.md
- HTTP Headers:

```yaml
Content-Insertion-Position: beginning
Heading: 標題
Heading-Boundary: ::
```

- Content:

  ```yaml
  -  {{date}} [{{page.title}}]({{page.url}})
  ```
 
### 6.4. Append to current daily note

- POST /periodic/daily/
- Content:

```yaml
## 2. {{page.title}}
URL: {{page.url}}
{{#if page.selectedText}}

{{quote page.selectedText}}
{{/if}}
```

### 6.5. Capture page snapshot

- PUT /vault/{{filename page.title}}.md
- Content:

```markdown
---
page-title: {{json page.title}}
url: {{page.url}}
date: "{{date}}"
---
{{#if page.selectedText}}

{{quote page.selectedText}}

---

{{/if}}{{page.content}}
```

### 6.6. Append to existing note

- POST (空值)
- Content:

```yaml
## {{date}}
{{#if page.selectedText}}

{{quote page.selectedText}}
{{/if}}
```

## 7. AutoHotkey整合腳本

用 `AutoHotkey` 腳本在命令行直接產生訊息到指定的筆記。最簡單的方法是用`Run` 執行 `cUrl.exe`，下方腳本則是使用 `COM` 物件來調用。

- [腳本原始檔](https://gist.github.com/emisjerry/6a14b86caebdc37ff963337848709269)

```AutoHotkey
/* 
;; fleet.ahk "message"
;; Windows cmd.exe 多行分隔號用 ^
curl -X "POST" ^
  "https://127.0.0.1:27124/vault/030-Inbox/fleet.md" ^
  -H "accept: */*" ^
  -H "Authorization: Bearer 4c1f7a807f92a153944269bfd610571970a887b3952a9b2161b9cb98e9f8708a" ^
  -H "Content-Type: text/markdown" ^
  -d "## This is my document something else here"
*/
#SingleInstance Force

MSG = %1%
if (MSG == "") {
  MsgBox Please add message.
  return
}
API_KEY := "4c1f7a807f92a153944269bfd610571970a887b3952a9b2161b9cb98e9f8708a"
URL := "https://127.0.0.1:27124/vault/030-Inbox/fleet.md"
;;TEXT := "# 測試筆記`n`nLocal REST API 測試`n" . "Time: " . A_HOUR . A_MIN . A_SEC
TEXT =
(
## %A_YYYY%:%A_MM%:%A_DD% %A_HOUR%:%A_MIN%:%A_SEC%

%MSG%

---

)

oHTTP := ComObjCreate("WinHttp.WinHttpRequest.5.1")
oHTTP.Open("POST", URL , False)
oHTTP.SetRequestHeader("Content-Type", "text/markdown")
oHTTP.SetRequestHeader("Authorization", "Bearer " . API_KEY) 
oHTTP.Send(TEXT)

sResponse := oHTTP.ResponseText
if (sResponse == "") {
  oHTTP.Open("GET", URL , False)
  oHTTP.SetRequestHeader("Content-Type", "text/markdown")
  oHTTP.SetRequestHeader("Authorization", "Bearer " . API_KEY)
  oHTTP.Send()
  sResponse := oHTTP.ResponseText
}

;; w=width, h=height, r=rows
Gui, Add, Edit, w400 h100 r10, % sResponse
Gui, Add, Edit, w400 h100 r10, % oHTTP.GetAllResponseHeaders
Gui, Show, AutoSize Center
return
 
GuiClose:
ExitApp
```

## 8. 相關鏈接

- [Local REST API 外掛 GitHub coddingtonbear/obsidian-local-rest-api: Get, change, and otherwise interact with your notes in Obsidian via a REST API.](https://github.com/coddingtonbear/obsidian-local-rest-api)
- [Obsidian Web 擴充 GitHub coddingtonbear/obsidian-web: Connect your web browser with your notes in Obsidian.](https://github.com/coddingtonbear/obsidian-web)
- [Local Rest API for Obsidian: Interactive API Documentation](https://coddingtonbear.github.io/obsidian-local-rest-api/)

## 9. 教學影片

![](https://youtu.be/sPqgHcuT_Sw)







